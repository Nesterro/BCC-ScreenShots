<Window x:Class="BCCScreenShot.ScreenSnippetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выделение области экрана" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" Cursor="Cross"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        KeyDown="Window_KeyDown">

    <Canvas x:Name="OverlayCanvas" Background="#70000000">
        <!-- Selection Rectangle -->
        <Rectangle x:Name="SelectionRect" Stroke="#6366F1" StrokeThickness="2" StrokeDashArray="4 2" Fill="#10FFFFFF" Visibility="Collapsed"/>

        <!-- Instruction & Dimensions Banner -->
        <Border x:Name="InfoBanner" Background="#1E293B" CornerRadius="8" Padding="12,8" Canvas.Left="20" Canvas.Top="20" BorderBrush="#6366F1" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="✂️ Зажмите ЛКМ и выделите область на экране (Esc для отмены)" Foreground="#F8FAFC" FontSize="13" FontWeight="SemiBold"/>
                <TextBlock x:Name="TxtDimensions" Text="" Foreground="#06B6D4" FontSize="13" FontWeight="Bold" Margin="15,0,0,0"/>
            </StackPanel>
        </Border>
    </Canvas>
</Window>
